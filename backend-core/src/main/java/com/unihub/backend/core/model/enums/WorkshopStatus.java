package com.unihub.backend.core.model.enums;

public enum WorkshopStatus {
    ACTIVE, CANCELLED
}

// Separate files would be better but I'll combine if appropriate or create multiple files if I can.
// Actually, I'll create separate files.
